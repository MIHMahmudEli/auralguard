# Copy to .env.local and fill in.
# URL of the AuralGuard HF Space (or local uvicorn server).
AURALGUARD_API_URL=https://MIHMahmudEli-auralguard.hf.space
# Optional bearer token if the Space is private.
# AURALGUARD_API_TOKEN=hf_xxx
