# @package data
name: wavefake
sample_rate: 16000
crop_seconds: 4.0
random_crop: false
pad_mode: repeat

manifests:
  eval: data/manifests/wavefake.csv

num_workers: 8
pin_memory: true

augment:
  enabled: false
