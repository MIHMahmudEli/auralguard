# data/

- `raw/`        downloaded corpora (gitignored — never commit audio)
- `processed/`  cached features (gitignored)
- `manifests/`  normalized CSV lists (committed — the only thing that lives in git here)

See `../docs/DATASETS.md` for the schema and download recipes.
