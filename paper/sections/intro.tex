% Introduction — outline (write prose here)
% 1. Threat: TTS/VC realism -> fraud, disinformation, biometric bypass.
% 2. The gap: in-domain EER is solved (<1%), but detectors fail on
%    (G1) unseen generators, (G2) real-world channels/codecs, (G3) calibration/XAI.
% 3. Reframe: open-set, channel-robust, calibrated detection.
% 4. Contributions (mirror docs/RESEARCH_PLAN.md Section 1):
%    (i) AuralGuard multi-view architecture;
%    (ii) one-class + supervised-contrastive objective;
%    (iii) channel-robustness augmentation curriculum;
%    (iv) cross-corpus + robustness + calibration benchmark protocol;
%    (v) open release (code, weights, API, UI).
