% Method — outline (see docs/ARCHITECTURE.md for the diagram + shapes)
% 3.1 Problem formulation: open-set binary detection, score s, threshold tau.
% 3.2 View A: SSL front-end + learnable layer attention (Eq. for weighted sum).
% 3.3 View B: LFCC + modified group-delay + CQT-phase -> light CNN.
% 3.4 Fusion: gated cross-attention.
% 3.5 Back-end: spectro-temporal graph attention (AASIST).
% 3.6 Objective: OC-Softmax (define margins) + SupCon auxiliary + layer-entropy reg.
% 3.7 Augmentation curriculum: RawBoost + codec chain + noise/reverb.
% Include the loss equation from docs/ARCHITECTURE.md.
